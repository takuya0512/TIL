# 素数を小さい方から6つ並べると2,3,5,7,11,13であり、6番目の素数は13です。
# 101番目の素数を求めてください。

# 解答解説
# 基本的な解き方は問7と変わりませんが、もしも調査した数が素数だった場合は「<<」を使って配列の末尾にその数字を追加し、
# 次の繰り返しで利用します。
# 模範解答では素数かどうかの判定は割り切れた時点で「素数かどうか」をtrue/falseで表す変数に保持するようにしています。
# 割り切れた時点で「break」を使って繰り返しを抜けて次の処理にうつることで、全て調査するよりも判定の速度が早くなります。
# また、偶数はかならず2で割り切れるため素数ではないので、調査する数字は奇数のみとしています。

primes = [2]  # 2は最初から素数リストに含めておく
num = 3  # 3から素数を探していく

while primes.length < 101
  is_prime = true
  primes.each do |prime|
    if num % prime == 0
      is_prime = false
      break
    end
  end
  primes << num if is_prime
  num += 2  # 奇数だけ調べる
end

primes.last
